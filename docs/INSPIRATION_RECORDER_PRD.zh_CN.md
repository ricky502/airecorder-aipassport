# AI Passport 灵感记忆卡：开发蓝图

## 完成定义

用户按实体按键即可录下灵感；设备离线保存、联网后按分钟滚动上传；后端确认后才删除音频；Passport 与旧录音卡共用同一后端，后端将语音交给同一 Feishu Agent。设备可以在重启后继续上传未完成片段。

## 固定交互

- `OK` 单击：开始录音、暂停、继续。
- `DOWN` 单击：停止并封存当前片段。
- `UP`：预留给中间主屏小程序。
- 没有触屏录音按钮。
- 顶栏：时间/日期、电量；不显示 Wi-Fi。
- 中间：留给后续的情绪卡、电子宠物等轻量小程序。
- 底栏：录制/暂停/停止图标、短音量波形、`WIFI READY/OFFLINE`、剩余 `CACHE`。

## 数据与能耗契约

- 音频：8 kHz、16-bit、单声道 IMA-ADPCM。
- 分片：60 秒；单片上传确认后才删除。
- 离线目标：至少 10 分钟。
- Wi-Fi：有待传分片时才打开；上传完成且无待传项立即断开并关闭。
- 身份安全：不得擦除或写入 `cardid@0x356000`，不得覆盖原厂资源区。

## 实施清单

| 阶段 | 交付 | 状态 |
| --- | --- | --- |
| 0 | 真机 Flash/分区只读验证 | 完成：ESP32-C3 rev 1.1、8MB；已避开 cardid 与原厂资源。 |
| 1 | ADPCM、状态机、60 秒分片、voicefs | 完成。 |
| 2 | 断电恢复、确认后删除、后台上传任务 | 完成；待真机编译/验证。 |
| 3 | Wi-Fi 上传窗口与手机 BLUFI 配网 | 上传窗口完成；BLUFI 配网接入中。 |
| 4 | 后端接收、ASR/Feishu Agent 统一处理 | 接收协议完成；复用旧录音卡的飞书处理链路。 |
| 5 | 录音卡正式 UI、NTP 时间 | UI 骨架完成；动态时间与真机显示待验证。 |
| 6 | 完整 ESP-IDF 构建、官方网页刷入、真机验收 | 未开始；绝不在未经验证的构建上刷机。 |

## 真机验收

1. OK 开始录音，底栏红色 `REC` 和短波形持续变化。
2. OK 暂停/继续，黄暂停图标正确变化；DOWN 停止，方块显示约 1 秒。
3. 断网连续录制超过 10 分钟，重启后缓存仍在。
4. 有网时，每完成一分钟，上一分钟后台上传；录音不中断。
5. 后端收到片段、确认成功后设备删除对应文件；网络失败不删。
6. Agent 在飞书返回结构化灵感结果。
7. 重启设备，不覆盖 `cardid`、原厂资源或未确认录音。
